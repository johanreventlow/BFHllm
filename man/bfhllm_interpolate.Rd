% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prompts.R
\name{bfhllm_interpolate}
\alias{bfhllm_interpolate}
\title{Interpolate Prompt Template}
\usage{
bfhllm_interpolate(template, data)
}
\arguments{
\item{template}{Character string, prompt template with \code{{{placeholders}}}}

\item{data}{Named list with values to interpolate}
}
\value{
Character string with interpolated prompt
}
\description{
Replaces placeholders in prompt template with values from data list.
Placeholders use \code{{{variable}}} syntax (double curly braces).
}
\details{
\strong{Placeholder Syntax:}
Use \code{{{variable_name}}} in template. Spaces inside braces are allowed
(\code{{{ variable }}} works).

\strong{Missing Variables:}
If a placeholder variable is not found in data, it is left unchanged
in the output (no error thrown).

\strong{Type Coercion:}
All values are coerced to character strings via \code{as.character()}.
}
\examples{
\dontrun{
# Basic interpolation
template <- "Hello {{name}}, you are {{age}} years old"
data <- list(name = "Alice", age = 30)
prompt <- bfhllm_interpolate(template, data)
# "Hello Alice, you are 30 years old"

# SPC prompt template
template <- paste(
  "Analyze this {{chart_type}} chart with {{n_points}} data points.",
  "The process shows {{variation_type}} variation.",
  "Suggest improvements."
)
data <- list(
  chart_type = "run chart",
  n_points = 24,
  variation_type = "stable"
)
prompt <- bfhllm_interpolate(template, data)
}

}
