% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/response-validation.R
\name{validate_response}
\alias{validate_response}
\title{Validate and Sanitize LLM Response}
\usage{
validate_response(text, max_chars = 350)
}
\arguments{
\item{text}{Character string, raw response from LLM}

\item{max_chars}{Integer, maximum allowed characters (default: 350)}
}
\value{
Character string with sanitized text, or NULL if invalid
}
\description{
Validates API response text and sanitizes it by:
\itemize{
\item Checking for NULL or empty responses
\item Removing HTML tags
\item Normalizing whitespace
\item Trimming to maximum character limit
\item Balancing markdown formatting
}
}
\details{
\strong{Sanitization Steps:}
\enumerate{
\item Check for NULL/empty input
\item Remove HTML tags
\item Normalize whitespace (collapse multiple spaces)
\item Trim to max_chars (preserving word boundaries)
\item Balance markdown asterisks (ensure even count)
}

\strong{Character Limit:}
When text exceeds max_chars, it is truncated at the last complete word
before the limit, and "..." is appended. Markdown formatting is balanced
to avoid broken bold/italic markers.
}
\examples{
\dontrun{
# Valid response
text <- validate_response(
  "This is a **valid** response",
  max_chars = 100
)

# Long response (will be truncated)
long_text <- paste(rep("word", 100), collapse = " ")
text <- validate_response(long_text, max_chars = 50)
}

}
