% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{bfhllm_cache_create}
\alias{bfhllm_cache_create}
\title{Create Generic In-Memory Cache}
\usage{
bfhllm_cache_create(ttl_seconds = 3600)
}
\arguments{
\item{ttl_seconds}{Numeric, time-to-live in seconds (default: 3600 = 1 hour)}
}
\value{
Cache object (environment) with methods:
\describe{
\item{get(key)}{Retrieve cached value, or NULL if not found/expired}
\item{set(key, value)}{Store value with current timestamp}
\item{clear()}{Remove all cached entries}
\item{stats()}{Return cache statistics}
}
}
\description{
Creates a generic cache object for storing LLM responses. Cache uses
hash-based keys and enforces TTL (time-to-live) on retrieval.
}
\details{
\strong{Cache Structure:}
Each cache entry is stored as:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{list(value = response_text, timestamp = Sys.time())
}\if{html}{\out{</div>}}

\strong{TTL Enforcement:}
Cache entries are checked on retrieval. If age exceeds TTL, NULL is returned
(cache miss). Expired entries are not automatically removed - use \code{clear()}
for manual cleanup.

\strong{Thread Safety:}
Cache is stored in an R environment. Not thread-safe - use separate cache
instances per thread/process.
}
\examples{
\dontrun{
# Create cache with 1 hour TTL
cache <- bfhllm_cache_create(ttl_seconds = 3600)

# Store value
cache$set("key1", "response text")

# Retrieve value
value <- cache$get("key1")

# Check stats
stats <- cache$stats()
print(stats$entries) # 1

# Clear cache
cache$clear()
}

}
